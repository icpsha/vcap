---
# Deployment
# ----------
deployment:
  name: "cloud_director"

jobs:
  install:
    - cloud_director
  installed:
    - nats_server:
        host: "172.31.4.13"
        port: "4222"
        user: "nats"
        password: "nats"
